import React from "react";
import translations from "../data/translations.json";

const Timetable = ({ language }) => {
  return <section className="container" id="time_table"></section>;
};

export default Timetable;
