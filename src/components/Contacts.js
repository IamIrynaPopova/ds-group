import React from "react";
import translations from "../data/translations.json";

const Contacts = ({ language }) => {
  return <section className="container сontacts"></section>;
};

export default Contacts;